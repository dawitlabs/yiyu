version: "2"

sql:
  - engine: postgres
    queries: "internal/adapter/repository/queries"
    schema: "migrations"
    gen:
      go:
        package: "repository"
        out: "internal/adapter/repository"
        emit_json_tags: true
        emit_db_tags: true
        emit_interface: true
